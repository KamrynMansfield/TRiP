% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_route_leaflet.R
\name{make_route_leaflet}
\alias{make_route_leaflet}
\title{Build the interactive route map}
\usage{
make_route_leaflet(routes_sf, county_sf)
}
\arguments{
\item{routes_sf}{An \code{sf} object of route geometries with a \code{route_id}
column, as returned by \code{\link[=get_gtfs_routes]{get_gtfs_routes()}}.}

\item{county_sf}{An \code{sf} object of county polygons, as returned by
\code{\link[=find_overlapping_counties]{find_overlapping_counties()}}, drawn as a grey reference layer.}
}
\value{
A \code{leaflet} htmlwidget, rendered by \code{renderLeaflet()} in
\code{app_server()}.
}
\description{
Draws the agency's bus routes over a light basemap, with the overlapping
county boundaries underneath for context. Each route gets its own color, and
hovering over a route highlights it in red and shows its \code{route_id} as a
tooltip so the user can confirm the IDs match their ridership file.
}
\examples{
\dontrun{
routes_sf <- get_gtfs_routes("gtfs.zip")
make_route_leaflet(routes_sf, find_overlapping_counties(routes_sf))
}
}
