% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_coefficients.R
\name{check_coefficients}
\alias{check_coefficients}
\title{Check model coefficient reasonableness}
\usage{
check_coefficients(model, extra_vars)
}
\arguments{
\item{model}{A model created to forecast bus ridership in the TRiP app,
typically the \code{fixest} object stored in \code{selected_model()}.}

\item{extra_vars}{A named character vector of any additional variables the
agency uploaded, in \code{c("Display Name" = "log_variable")} form, as produced
by the \code{addnl_vars()} reactive in \code{app_server()}. These are appended to the
lookup so custom columns get readable labels instead of \code{NA}.}
}
\value{
A \code{gt} table of coefficients that is colored to point out unexpected
signs. The helper \code{sign_check} column is hidden before rendering, and the
\code{Message} column only appears when at least one variable triggered a flag.
}
\description{
Builds a formatted coefficient table for the chosen model and colors any row
whose sign contradicts theory or prior research: red for an error (a sign
that should never occur, such as a negative VRM elasticity, which would mean
running more service reduces ridership) and yellow for a warning (a sign
worth a second look). Rows with no expectation on file are left uncolored.
}
\examples{
\dontrun{
check_coefficients(selected_model(), addnl_vars())
}
}
