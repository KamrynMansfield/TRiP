# list variables to be log transformed three variables not log transformed: "workers_16_over", "median_earnings", "emp_pop_ratio"

list variables to be log transformed three variables not log
transformed: "workers_16_over", "median_earnings", "emp_pop_ratio"

## Usage

``` r
make_model_data_frame(data_xlsx, acs_data, gas_data, fare_df = NULL)
```
