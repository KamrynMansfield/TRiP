# add scenarios to forecast_grid and get elasticity factors

add scenarios to forecast_grid and get elasticity factors

## Usage

``` r
forecast_ridership(
  coefs,
  data_xlsx,
  acs_data,
  gas_csv,
  scenario_inputs_df,
  start_year = NULL,
  start_month = NULL,
  fare_df = NULL,
  brt_df = NULL
)
```
